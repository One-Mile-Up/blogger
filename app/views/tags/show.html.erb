<h1>Tag: <%= @tag.name %></h1>

<h2>Articles:</h2>
<ul>
  <% @tag.articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>

<%= link_to "Delete this tag", tag_path(@tag), method: :delete %>
